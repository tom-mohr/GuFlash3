<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:icon="@drawable/ic_menu_filter"
        android:title="Filter"
        app:showAsAction="always">
        <menu>
            <group
                android:checkableBehavior="single">
                <item
                    android:checked="true"
                    android:id="@+id/filter_sort_distance"
                    android:icon="@drawable/ic_menu_distance"
                    android:title="sortiere nach Entfernung"
                    app:showAsAction="never" />
                <item
                    android:id="@+id/filter_sort_time"
                    android:icon="@drawable/ic_menu_clock"
                    android:title="sortiere nach Zeit"
                    app:showAsAction="never" />
                <item
                    android:id="@+id/filter_sort_alphabetically"
                    android:icon="@drawable/ic_menu_sort_alphabetically"
                    android:title="sortiere alphabetisch"
                    app:showAsAction="never" />
            </group>
            <group
                android:checkableBehavior="all">
                <item
                    android:id="@+id/filter_ignore_distance"
                    android:title="nicht weiter als 10 km"
                    app:showAsAction="never" />
                <item
                    android:id="@+id/filter_ignore_time"
                    android:title="nicht später als 10 Tage"
                    app:showAsAction="never" />

            </group>
        </menu>
    </item>

</menu>
